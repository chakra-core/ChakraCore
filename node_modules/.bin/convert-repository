#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../@feathersjs/tools/bin/convert-repository" "$@"
  ret=$?
else 
  bash  "$basedir/../@feathersjs/tools/bin/convert-repository" "$@"
  ret=$?
fi
exit $ret
