#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ember-source-channel-url/bin/ember-source-channel-url" "$@"
  ret=$?
else 
  node  "$basedir/../ember-source-channel-url/bin/ember-source-channel-url" "$@"
  ret=$?
fi
exit $ret
